import logo from '../../assets/harsh-logo.png';
const AppLogo =(props)=>{
    return(
        <div>
            <img src={logo} alt="logo" style={{width:100+ "px", padding:1+'rem'}} />
        </div>
    )
}

export default AppLogo;